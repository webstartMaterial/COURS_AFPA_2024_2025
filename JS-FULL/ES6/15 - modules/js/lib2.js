const learn_language = language => console.log(`J'apprend le langage : ${language}`);
const nationality = "Spanish";
const cut_beard = firstName => console.log(`${firstName} a regretté d'avoir coupé sa barbe`);

export {learn_language, nationality, cut_beard};

